services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: forcreators
      POSTGRES_USER: forcreators
      POSTGRES_PASSWORD: forcreators
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
